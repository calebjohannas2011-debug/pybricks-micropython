name: Build Custom Boost Firmware

on:
  workflow_dispatch: # This adds a manual "Run" button to your GitHub dashboard

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install ARM GCC Toolchain
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-arm-none-eabi libnewlib-arm-none-eabi

    - name: Patch Move Hub Config
      run: |
        CONFIG_FILE="ports/stm32/boards/move_hub/mpconfigport.h"
        
        # Append configurations to enable usys/uselect and disable large features
        cat << 'EOF' >> "$CONFIG_FILE"
        
        /* Custom Additions for usys and uselect */
        #undef MICROPY_PY_SYS
        #define MICROPY_PY_SYS (1)
        
        #undef MICROPY_PY_USELECT
        #define MICROPY_PY_USELECT (1)
        
        /* Disabling heavy features to make space in Flash memory */
        #undef PYBRICKS_PY_EV3DEVICES
        #define PYBRICKS_PY_EV3DEVICES (0)
        
        #undef MICROPY_PY_BUILTINS_REVERSED
        #define MICROPY_PY_BUILTINS_REVERSED (0)
        
        #undef MICROPY_PY_BUILTINS_STR_COUNT
        #define MICROPY_PY_BUILTINS_STR_COUNT (0)
        EOF

    - name: Build Firmware
      run: |
        cd ports/stm32
        make BOARD=move_hub clean
        make BOARD=move_hub

    - name: Upload Firmware Artifact
      uses: actions/upload-artifact@v4
      with:
        name: custom-boost-firmware
        path: ports/stm32/build-move_hub/firmware.zip
